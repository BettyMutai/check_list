<%= form_for task,remote: true do |f| %>
  <%= f.check_box :complete, remote: true %>
  <%#= f.submit "Update" %>
  <%#= f.label :complete, task.name %>
  <%= best_in_place task, :name, tabindex: "1" %>
  <%= link_to '(remove)', task, method: :delete, data: {confirm: 'Are you sure'}, remote: true %>
<% end %>
